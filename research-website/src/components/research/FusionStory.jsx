const factors=['availability','recency','coverage','reliability','validation evidence']
const states=['strong','stale','incomplete','unavailable','excluded']
export default function FusionStory(){return <section className="research-section fusion-section" data-section="fusion"><div className="shell fusion-grid"><div><p className="eyebrow">WHY MULTIMODAL?</p><h2>More data is not automatically better evidence.</h2><p>Eligible signals reach fusion only after quality and evidence checks. A weak or unavailable stream should reduce certainty rather than silently become a low value.</p><div className="state-row">{states.map(s=><span key={s}>{s}</span>)}</div></div><div className="fusion-equation">{factors.map((f,i)=><div key={f}><span>{f}</span>{i<factors.length-1&&<b>+</b>}</div>)}<i>↓</i><strong>usable contribution</strong><small>The system may return insufficient evidence.</small></div></div></section>}
