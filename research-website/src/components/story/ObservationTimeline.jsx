import { researchNarrative } from '../../data/research'
export default function ObservationTimeline(){return <section className="story-section observation-section" data-section="observation"><div className="shell"><p className="eyebrow">WHAT MAKES CONTINUOUS OBSERVATION DIFFICULT?</p><h2>What we can observe is only part of the picture.</h2><div className="observation-tracks">{researchNarrative.timescales.map(([time,label],i)=><article key={time}><span className="track-index">0{i+1}</span><div className="track-line"><i/></div><div><small>{time}</small><h3>{label}</h3></div></article>)}</div><p className="section-note">Different evidence operates at different timescales.</p></div></section>}
