import { motion, useReducedMotion } from 'framer-motion'
import { researchNarrative } from '../../data/research'
import MotionReveal from '../motion/MotionReveal'
export default function ObservationTimeline(){const reduced=useReducedMotion();return <section className="story-section observation-section" data-section="observation"><MotionReveal className="shell"><p className="eyebrow">WHAT MAKES CONTINUOUS OBSERVATION DIFFICULT?</p><h2>What we can observe is only part of the picture.</h2><div className="observation-tracks">{researchNarrative.timescales.map(([time,label],i)=><motion.article key={time} initial={reduced?false:{opacity:0,x:-24}} whileInView={reduced?undefined:{opacity:1,x:0}} viewport={{once:true,amount:.65}} transition={{duration:.55,delay:i*.08}} whileHover={reduced?undefined:{x:6}}><span className="track-index">0{i+1}</span><div className="track-line"><i/></div><div><small>{time}</small><h3>{label}</h3></div></motion.article>)}</div><p className="section-note">Different evidence operates at different timescales.</p></MotionReveal></section>}
