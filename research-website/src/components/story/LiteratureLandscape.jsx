import { motion, useReducedMotion } from 'framer-motion'
import { literatureThemes } from '../../data/literature'
import MotionReveal from '../motion/MotionReveal'
export default function LiteratureLandscape(){const reduced=useReducedMotion();return <section className="story-section literature-section" data-section="literature"><div className="shell"><MotionReveal><p className="eyebrow">02 / WHAT RESEARCH ALREADY TELLS US</p><div className="section-title"><h2>A literature landscape, not a wall of citations.</h2><p>This public synthesis follows the project research documentation. Specific reference records are withheld here unless a verified public citation artifact is available.</p></div></MotionReveal><div className="literature-list">{literatureThemes.map((item,i)=><motion.article key={item.theme} initial={reduced?false:{opacity:0,y:18}} whileInView={reduced?undefined:{opacity:1,y:0}} viewport={{once:true,amount:.45}} transition={{duration:.5,delay:Math.min(i*.05,.25)}} whileHover={reduced?undefined:{x:7,backgroundColor:'rgba(255,255,255,.38)'}}><span>0{i+1}</span><div><h3>{item.theme}</h3><p>{item.demonstrates}</p></div><div><small>Typical limitation</small><p>{item.limitations}</p></div><div><small>Project relevance</small><p>{item.relevance}</p></div></motion.article>)}</div></div></section>}
