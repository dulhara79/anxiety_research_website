import { useEffect,useState } from 'react'
import { NavLink } from 'react-router-dom'
import { Menu,X } from 'lucide-react'
const nav=[['Overview','/'],['Research','/research'],['Components','/components'],['System','/system'],['Findings','/findings'],['Documents','/documents'],['Team','/team']]
export default function Navbar(){const [open,setOpen]=useState(false);useEffect(()=>{const close=e=>e.key==='Escape'&&setOpen(false);window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[]);return <header className="site-header"><div className="nav-shell"><NavLink to="/" className="brand" onClick={()=>setOpen(false)}><strong>R26—DS—012</strong><span>Multimodal Anxiety Research</span></NavLink><button className="menu" type="button" aria-label={open?'Close navigation':'Open navigation'} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X size={20}/>:<Menu size={20}/>}</button><nav aria-label="Primary" className={open?'nav-links open':'nav-links'}>{nav.map(([n,p])=><NavLink key={p} to={p} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}>{n}</NavLink>)}<a className="explore-link" href="#/research" onClick={()=>setOpen(false)}>Explore Research</a></nav></div></header>}
