import { Link } from 'react-router-dom'
import { findings } from '../../data/findings'
export default function FindingsPreview({limit=5}){return <section className="findings-section" id="findings" data-section="findings"><div className="shell"><p className="eyebrow">04 / WHAT WE FOUND</p><div className="section-title"><h2>Results with their evaluation conditions attached.</h2><p>The visual language becomes quieter here: metric, setting and interpretation stay together.</p></div><div className="findings-table">{findings.slice(0,limit).map(item=><article key={`${item.component}-${item.metric}`}><span>{item.component}</span><div><small>{item.context}</small><h3>{item.metric}</h3></div><b>{item.value}</b><p>{item.interpretation}</p></article>)}</div><Link className="text-link" to="/findings">Explore all findings →</Link></div></section>}
