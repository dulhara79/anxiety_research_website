import { project } from '../data/research'
const docs=[['Research repository','Current implementation, research results and source-of-truth technical records.',project.repository],['Website repository','Research experience source, interaction system and design history.',project.websiteRepository]]
export default function Documents(){return <main className="internal-page paper-page"><header className="page-hero shell-wide paper-hero"><p className="eyebrow">DOCUMENTS</p><h1>Research documents</h1><p>A compact index of project sources and implementation records.</p></header><section className="shell-wide document-records">{docs.map(([name,description,url],i)=><article key={name}><span>{String(i+1).padStart(2,'0')}</span><div><h2>{name}</h2><p>{description}</p></div><a href={url} target="_blank" rel="noreferrer" data-cursor="OPEN ↗">Open ↗</a></article>)}</section></main>}
