import MotionReveal from '../components/motion/MotionReveal'
import SignalVisual from '../components/visualizations/SignalVisual'
import { evidence } from '../data/research'

export default function Results(){return <main className="internal-page paper-page"><header className="page-hero shell-wide paper-hero"><p className="eyebrow">03 / EVIDENCE</p><h1>Evidence record</h1><p>Validation results are paired with the setting that produced them and the limitation that changes how they should be interpreted.</p></header><section className="shell-wide results-record"><div className="evidence-table" role="table" aria-label="Research evidence record"><div className="evidence-table-head" role="row"><span role="columnheader">Component</span><span role="columnheader">Measure</span><span role="columnheader">Result</span><span role="columnheader">Interpretation</span></div>{evidence.map((item,i)=><MotionReveal className="evidence-table-row" key={`${item.component}-${item.metric}`} delay={i*.04}><span>{item.component}</span><div><strong>{item.metric}</strong><small>{item.context}</small></div><b>{item.value}</b><p>{item.note}</p></MotionReveal>)}</div></section><section className="validation-gate-section"><div className="shell-wide validation-gate-grid"><MotionReveal><p className="eyebrow light">VALIDATION GATE</p><h2>C2 is excluded from active fusion.</h2><p>The final GLOBEM GATv2 result was not distinguishable from chance under the project's leakage-free held-out evaluation. The reading remains part of the evidence record, while its current active fusion weight remains <strong>0.0</strong>.</p><p className="gate-principle">Evidence-based modality gating is an intended research behavior, not a software failure.</p></MotionReveal><MotionReveal className="gate-visual" delay={.08}><SignalVisual type="c2"/></MotionReveal></div></section></main>}
