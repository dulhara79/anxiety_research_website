import MotionReveal from '../components/motion/MotionReveal'
import SignalVisual from '../components/visualizations/SignalVisual'
import { fusionEquation, methodology } from '../data/research'

export default function Methodology(){return <main className="internal-page paper-page"><header className="page-hero shell-wide paper-hero"><p className="eyebrow">04 / METHODOLOGY</p><h1>Evaluation first, integration second.</h1><p>A modality earns influence through evidence rather than through architectural symmetry.</p></header><section className="methodology-flow shell-wide"><article className="method-feature"><div><p className="eyebrow">02 / LEAKAGE CONTROL</p><h2>Grouped identities do not cross the evaluation boundary.</h2><p>{methodology[1].body}</p></div><SignalVisual type="leakage"/></article><article className="method-feature fusion-method"><div><p className="eyebrow">03 / RELIABILITY-WEIGHTED FUSION</p><h2>Availability is not the same as eligibility.</h2><p>{methodology[2].body}</p><div className="equation" aria-label="Reliability weighted fusion equations">{fusionEquation.map((line)=><code key={line}>{line}</code>)}</div><p className="equation-note">ω: deployment-relevant modality weight · ρ: recency factor · c: confidence / reliability term · unavailable or ineligible evidence is masked.</p></div><SignalVisual type="c4"/></article></section><section className="method-index shell-wide">{methodology.map((section,i)=><MotionReveal key={section.id} className="method-index-row" delay={i*.035}><span>{section.id}</span><div><h2>{section.title}</h2><p>{section.body}</p></div></MotionReveal>)}</section></main>}
